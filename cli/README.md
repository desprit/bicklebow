# This module contains utility functions to administrate the project

```sh
# Create database and tables
cargo run init-db
# Drop database
cargo run drop-db
# Populate Markets table
cargo run fill-db
```
